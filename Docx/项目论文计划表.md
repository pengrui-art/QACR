# Query-Adaptive Compute Routing (QACR) 论文写作计划表

## 📌 记录与打卡规则（非常重要）
1. 本计划表按论文写作的不同阶段划分，每个子项目前都有一个 checklist (`[ ]`)。
2. 当完成一个模块或草稿时，请将 `[ ]` 修改为 `[x]`。
3. **必须在完成的任务下方追加记录以下内容（形成写作日志）：**
   - **核心要点/结论**：简要描述该部分写了什么核心洞见，或者贴上该部分最关键的一句话 summary（如核心贡献的表述）。
   - **文档/图表路径**：附上对应 Overleaf 链接、Markdown 草稿路径、或是本地图表（如 PDF/Visio/PPT）的文件路径。
   - **遗留问题/后续修改**：记录自己或合作者觉得还需要在下一版（Revision）中润色的点。

---

## Phase 0：投稿定位与 Reviewer 防守（当前最高优先级）
**目标**：在正式写正文之前，先把“我们和现有工作到底差在哪、为什么不是换皮 pruning、reviewer 最可能打哪里”说清楚。**

- [ ] **任务 0.1：锁定论文主张与一句话 Thesis**
  - 要求：把论文主张收紧成一句可验证的话，避免泛泛而谈。推荐方向：
    - 不是“我们也会 query-guided token selection”
    - 而是“在统一预算下，query-conditioned multi-path compute allocation 比 binary pruning / region compression 更能保住关键细粒度信息”
  - 写作要求：
    - 给出 `main claim`、`non-claim`、`safe claim` 三个版本；
    - 明确哪些结论能写进主文，哪些只能写成潜力或 limitation。
  - *【完成记录】*
    - 核心要点：
    - 文档/图表路径：
    - 遗留问题：

- [ ] **任务 0.2：构建最接近相关工作的对照矩阵**
  - 要求：围绕最接近的工作建立 structured comparison table，至少包含：
    - `LVPruning`
    - `CROP`
    - `Script`
    - `SPIDER`
    - `FlashVLM`
    - `DyRate`
  - 表格列建议：
    - 是否 query/text-guided
    - 是否 token pruning
    - 是否 region-level compression
    - 是否 multi-path depth routing
    - 是否显式 budget-constrained
    - 是否真实 conditional execution
    - 是否 training-free / training-based
  - 目标：让 reader 一眼看出 QACR 真正剩余的新意在哪里。
  - *【完成记录】*
    - 核心要点：
    - 文档/图表路径：
    - 遗留问题：

- [ ] **任务 0.3：整理 Reviewer 攻击点与对应证据**
  - 要求：提前写一个“reviewer attack matrix”，至少覆盖：
    - “这不就是 query-guided pruning 吗？”
    - “为什么 latency 没有更好？”
    - “为什么 low-resolution 已经够了？”
    - “hard routing 会不会 collapse？”
    - “是不是只在 toy/proxy setup 上成立？”
  - 每个攻击点下必须对应：
    - 需要哪张主表/哪张图反击；
    - 如果证据不足，正文中该如何降调表述。
  - *【完成记录】*
    - 核心要点：
    - 文档/图表路径：
    - 遗留问题：

---

## Phase 1：核心故事线与大纲 (Storyline & Outline)
**目标**：明确论文的叙事逻辑，确保“从 token pruning 到 compute allocation”的核心转变清晰易懂，不被误解为传统的特征筛选。

- [ ] **任务 1.1：Abstract 草稿编写**
  - 要求：用 5-6 句话说明背景（高分辨率计算代价大）、痛点（传统 pruning 固定丢弃特征限制了动态能力）、方法（Query-adaptive compute routing）、结果（同预算下性能更好/效率更高）。
  - *【完成记录】*
    - 核心要点：
    - 文档/图表路径：
    - 遗留问题：

- [ ] **任务 1.2：Introduction 叙事逻辑构建**
  - 要求：画一个对比图（Figure 1：Traditional Pruning vs. Query-Adaptive Routing），生动展示对于相同的输入，不同 Query 会让计算重心发生转移。明确列出 3-4 点 Contributions（参考指导文档 3.3）。
  - 新增硬要求：
    - 引言里必须正面承认 2025-2026 年已经出现大量 `query/text-guided pruning/compression` 工作；
    - 你们的切入点要明确写成 `compute allocation under explicit budget`，而不是泛化成 `query-aware token selection`；
    - 第一页就埋下“为什么 binary pruning 不够”的动机，最好配合 OCR / small-object / multi-region 干扰示意图。
  - *【完成记录】*
    - 核心要点：
    - 文档/图表路径：
    - 遗留问题：

- [ ] **任务 1.3：Related Work 框架梳理**
  - 要求：梳理“Vision-Language Models”、“Token Pruning/Merging”、“Dynamic Neural Networks / Conditional Computation”三个主要流派，并严格界定我们的工作与它们的不同。
  - 新增硬要求：
    - 单独成段讨论 `LVPruning / CROP / Script / FlashVLM / SPIDER / DyRate`；
    - 不能只写“这些方法大多做 pruning”，必须写清它们已经覆盖到什么程度，以及 QACR 还剩下什么独特点；
    - Related Work 结束处要给出一个收束句：`existing methods mainly decide which tokens to keep, while QACR studies how much computation each retained region should receive under an explicit budget.`
  - *【完成记录】*
    - 核心要点：
    - 文档/图表路径：
    - 遗留问题：

---

## Phase 2：方法论与公式化 (Methodology Formulation)
**目标**：把代码实现包装成严谨的数学表达，强调“训练可导”与“资源受限”两个关键点。

- [ ] **任务 2.1：总体架构图绘制 (Method Figure)**
  - 要求：绘制主干流程图：`Image + Query → Coarse Encoder → Query-Adaptive Router → Multi-Path Execute (Skip, Shallow, Deep) → Fusion → MLLM`。
  - 新增硬要求：
    - 图中要显式画出 `budget controller / route cost`；
    - 若最终实现了真实条件执行，需要在图中明确区分 `training-time soft mixture` 与 `inference-time conditional execution`。
  - *【完成记录】*
    - 核心要点：
    - 文档/图表路径：
    - 遗留问题：

- [ ] **任务 2.2：Compute Routing 的数学建模**
  - 要求：写清楚 Token 选择本质上是一个路由问题。定义 $\mathcal{R}$ 路由空间（Skip, Shallow, Deep 三分支）。
  - 新增硬要求：
    - 除了 route space，还要定义 `route cost`、`expected compute`、`matched-budget comparison`；
    - 数学表述要能自然支持后面的 Pareto 和公平对比实验，而不是停留在抽象概念上。
  - *【完成记录】*
    - 核心要点：
    - 文档/图表路径：
    - 遗留问题：

- [ ] **任务 2.3：Soft-to-Hard Routing 与训练优化**
  - 要求：详细介绍训练阶段如何使用 Gumbel-Softmax 或 Soft-routing，以及推理时如何退化为 Hard routing。写出完整的 Loss Formulations（Task Loss + Compute Regularization/Budget Penalty）。
  - 新增硬要求：
    - 若后续加入防塌缩或一致性损失，要在这一节统一包装，不要分散到实验里才解释；
    - 必须为 reviewer 提前回答 `train-test mismatch`：为什么 soft 训练不会让 hard 推理失真。
  - *【完成记录】*
    - 核心要点：
    - 文档/图表路径：
    - 遗留问题：

---

## Phase 3：实验分析与结果展示 (Experiments & Results)
**目标**：用详实的数据与图表回答“核心假设是否成立”，击败竞争对手。

- [ ] **任务 3.1：实验设置 (Experimental Setup) 编写**
  - 要求：写清楚基座模型、数据集、评价指标（Accuracy 还要带上 FLOPs / Latency）。
  - 新增硬要求：
    - 单独写一段 `Fair Comparison Protocol`；
    - 明确说明同 backbone、同分辨率、同 token 数、同硬件、同 batch size、同 budget 点；
    - 明确哪些 baseline 是 training-free，哪些是 training-based，避免 reviewer 质疑“不公平”。
  - *【完成记录】*
    - 核心要点：
    - 文档/图表路径：
    - 遗留问题：

- [ ] **任务 3.2：Main Results (vs. Pruning & Baseline) 表格**
  - 要求：制作核心大表。必须至少有一列 FLOPs 和一列 性能指标。重点高亮在相近算力下，QACR 优于强降采样和 Token Pruning 基线。
  - 新增硬要求：
    - 主文至少放两张表：
      - `matched compute`
      - `matched latency`
    - 主文至少放两张图：
      - `Accuracy-Compute Pareto`
      - `Accuracy-Latency Pareto`
    - 主表 baseline 至少覆盖：
      - `LowRes`
      - `TokenPruning`
      - `ImageOnly`
      - 最接近的外部方法 `LVPruning / CROP / Script` 中的可复现者
    - 结论表述要分开：
      - “稳定优于 pruning family”
      - “是否优于 low-resolution”
      - “是否真的得到 latency gain”
  - *【完成记录】*
    - 核心要点：
    - 文档/图表路径：
    - 遗留问题：

- [ ] **任务 3.3：消融实验 (Ablation Studies)**
  - 要求：
    1. Query 的必要性（Image-only vs. Query-adaptive）；
    2. Budget 惩罚项的影响；
    3. Soft-to-Hard 鸿沟验证。
  - 新增硬要求：
    - 把 `key-token protection` 和 `hard collapse` 提升到正文级别，而不是只放附录；
    - 至少新增三类指标：
      - `key-token recall / miss rate`
      - `hard_collapse_ratio`
      - `mean_hard_minus_soft_gap`
    - 最好加入“same image, different query”控制实验，证明提升来自 query-conditioned compute allocation。
  - *【完成记录】*
    - 核心要点：
    - 文档/图表路径：
    - 遗留问题：

- [ ] **任务 3.4：直观可视化 (Visualizations)**
  - 要求：在正文中放入最博人眼球的**定性案例**：同一张图，不同的 Query 下 Token 计算深度的热力图对比。以及不同 Budget 下分支比例折线图。
  - 新增硬要求：
    - 至少准备一组 `QACR vs TokenPruning vs 外部最强方法` 的并排可视化；
    - 至少准备一组 OCR / 小目标 / 多区域干扰 case；
    - 图注要直接说清：QACR 多给了哪个 query-relevant 区域深算力，以及这如何对应正确答案。
  - *【完成记录】*
    - 核心要点：
    - 文档/图表路径：
    - 遗留问题：

- [x] **任务 3.5：Failure Analysis 与 Limitation 防守稿**
  - 要求：把失败模式主动写出来，但写成“可控风险”而不是“致命缺陷”。
  - 必须覆盖：
    - low-resolution 何时足够强；
    - latency 为何可能不占优；
    - hard routing collapse 的剩余风险；
    - query-conditioned 方法在简单样本上的收益上限。
  - 写作目标：让 reviewer 觉得你们知道问题在哪，并且正文数据已经把最危险的质疑压住了。
  - *【完成记录】*
    - 核心要点：已整理成一份可直接服务论文正文与答辩的防守稿，核心策略不是回避负面结果，而是把问题收束成四个“可控风险”：1）`low-resolution` 是强基线，但并未否定 query-conditioned compute allocation 的问题价值；2）当前实现尚未形成真实 latency 闭环，因此不把贡献包装成“已实现系统加速”；3）`soft-to-hard` 平均 gap 可控，但 `hard routing collapse` 仍是开放优化问题；4）当前最严重短板不是“不会随 query 改变”，而是“关键 token 保护还不够准”。文稿中同时给出了适合正文使用的英文 `Failure Analysis` 与 `Limitations` 草稿，以及 reviewer 常见攻击点的安全回应口径。
    - 文档/图表路径：`Docx/QACR_Failure_Analysis_与_Limitation_防守稿.md`
    - 遗留问题：当前文稿仍主要基于 `proxy + profiling + corner-case` 结果，待 `official benchmark`、`true conditional execution`、`key-token protection` 三类证据更新后，需要再统一调节语气；若后续能稳定优于 `LowRes` 或得到真实 latency gain，应相应收紧 limitation 表述。

---

## Phase 4：润色、排版与提交 (Polishing & Submission)
**目标**：确保格式规范，行文流畅，达到顶会（CVPR/ICCV/ECCV 等）提交标准。

- [ ] **任务 4.1：Conclusion & Limitaiton**
  - 要求：诚实总结文章得失。Limitation 可以提当前仅在单轴（Depth）探索，未来将拓展至多轴或更高分辨率等。
  - *【完成记录】*
    - 核心要点：
    - 文档/图表路径：
    - 遗留问题：

- [ ] **任务 4.2：全篇语法与逻辑润色**
  - 要求：查重并使用 Grammarly / ChatGPT 等工具校对语法。确保所有“token elimination”一类的词汇替换为“compute allocation/routing”导向的词汇。
  - *【完成记录】*
    - 核心要点：
    - 文档/图表路径：
    - 遗留问题：

- [ ] **任务 4.3：参考文献排查与引用修正**
  - 要求：确保引用的统一性，重点引用的最新的 MLLM 和 Dynamic Computation 工作不能遗漏。
  - *【完成记录】*
    - 核心要点：
    - 文档/图表路径：
    - 遗留问题：

---

## Phase 5：Supplementary & 开源 (Appendix)
**目标**：提交之后的工作，提供充足的细节应对不可避免的 Reviewer 质疑。

- [ ] **任务 5.1：编写 Appendix**
  - 要求：补充训练超参表、更多不同场景的可视化 Case、Router 的额外开销详细推导等。
  - *【完成记录】*
    - 核心要点：
    - 文档/图表路径：
    - 遗留问题：

- [ ] **任务 5.2：GitHub 开源代码规整 (Anonymous submission)**
  - 要求：构建双盲审查用的匿名代码库，包含可复现的最基础的 scripts，准备 README 并打包。
  - *【完成记录】*
    - 核心要点：
    - 文档/图表路径：
    - 遗留问题：
